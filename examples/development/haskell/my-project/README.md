# Haskell

This example was generated using `stack`. To generate a similar example, you can copy the codex json and run `codex shell`.

Once your shell has activated, run:

```
stack new my-project
cd my-project
stack build
stack exec my-project-exe
```

For more details, check out the [Haskell + Codex Docs](https://www.khulnasoft/codex/docs/codex_examples/languages/haskell/)
